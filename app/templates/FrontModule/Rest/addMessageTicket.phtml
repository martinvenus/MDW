<?xml version="1.0" encoding="UTF-8"?>
{if isset($errors)}
<errors>
    {foreach $errors as $item}
    <error>
        <description>{$item}</description>
    </error>
    {/foreach}
</errors>
{else}
<response>
    <result>OK</result>
</response>
{/if}